
# Mailbox list:
{

    use NethServer::MailServer;
    my $ms = NethServer::MailServer->new();
    my @mailboxes = $ms->getMailboxes();

    if( @mailboxes ) {
	$OUT = join("\n", map { $_ . "\t\tok" } @mailboxes) . "\n";
    } else {
	$OUT = "# ..no mailbox defined\n";
    }
}

